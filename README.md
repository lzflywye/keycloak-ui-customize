# Keycloak UI customization that inherits the default theme

This is an example of customizing the Keycloak UI while inheriting the default theme.

## Installation of favicon, etc.

### Applying to the Account Console and Admin Console

favicon.svg -> `theme/<theme>/common/resources/img/favicon.svg`.

### Applying to login theme

favicon.ico -> `theme/<theme>/common/resources/img`.
